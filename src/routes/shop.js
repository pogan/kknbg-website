'use strict';

const express = require('express');
const router = express.Router();

// Pełny moduł sklepu powstaje w Fazie 7. Na razie placeholder,
// aby aplikacja bootowała i /sklep było osiągalne.

module.exports = router;
