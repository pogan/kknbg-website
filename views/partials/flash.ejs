<% if (typeof flash !== 'undefined' && flash) { %>
  <div class="flash flash--<%= flash.type === 'success' ? 'success' : 'error' %>" role="status" id="dziekujemy">
    <%= flash.text %>
  </div>
<% } %>
